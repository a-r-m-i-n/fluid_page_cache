# cat=Basic; type=boolean; label=Enable Standalone Backend Module: When enabled, the extension will be available as a standalone backend module, which allows you to visualize set page cache tags.
enableStandaloneBackendModule = 1
